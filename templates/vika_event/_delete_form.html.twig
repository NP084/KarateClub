<form method="post" action="{{ path('vika_event_delete', {'id': vikaEvent.id}) }}" onsubmit="return confirm('Etes vous certain de vouloir supprimer cet événement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vikaEvent.id) }}">
    <button class="btn btn-danger">Effacer</button>
</form>
