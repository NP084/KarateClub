{% extends 'base.html.twig' %}

{% block title %}Edit PriceGrid{% endblock %}

{% block Body %}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <h1>Modification d'un tarif</h1>
            <a href="{{ path('vika_event_edit',{'id':price_grid.vikaEvent.id}) }}"
               class="btn-btn-primary">
                <span class="badge badge-pill badge-warning">Retour</span>
            </a>
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <div class="form-group">
                                {{ include('price_grid/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                                {{ include('price_grid/_delete_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


    {% endblock %}